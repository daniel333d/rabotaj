/** Canonical production origin, used by robots.ts/sitemap.ts. Override via NEXT_PUBLIC_SITE_URL if the domain changes. */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://rabotaj.com";
